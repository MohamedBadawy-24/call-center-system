MONGO_URI=mongodb://127.0.0.1:27017/call-center
JWT_SECRET=change-me
PORT=3000

# Optional: comma-separated browser origins (admin-ui). When unset, CORS allows all (dev-friendly).
# CORS_ORIGIN=http://localhost:3001,http://127.0.0.1:3001

# Optional: Socket.IO allowed origins (defaults to CORS_ORIGIN or localhost:3001).
# SOCKET_IO_CORS_ORIGIN=http://localhost:3001

# Backend API (Express). No trailing slash.
# VITE_API_URL=http://localhost:3000

# Optional: Socket.IO server if different from API (defaults to VITE_API_URL).
# VITE_SOCKET_URL=http://localhost:3000

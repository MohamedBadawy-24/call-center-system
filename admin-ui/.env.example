# Leave VITE_API_URL unset (or empty) when using Vite dev server proxy for local network access.
# Only set this for production builds pointing to a deployed backend.
# VITE_API_URL=https://your-production-domain.com/api
VITE_API_URL=
